# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/

# Session signing key, generated on first run (see app/auth/tokens.py).
# Never commit this: anyone holding it can mint a token for any account.
data/.auth_secret

# Data (kept locally; reference CSV + raw snapshots are the audit trail
# and can be large — see data/reference/README.md for how to regenerate)
# data/airfare.db
# data/airfare.db-wal
# data/airfare.db-shm
# data/scrape.lock
# data/raw/*.jsonl

# Editors / OS
.DS_Store
